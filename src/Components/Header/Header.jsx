import "./Header.css"

function Header() {

    return(
        <header class="top-bar">
            <nav>
                <a>Home</a>
                <a>Experience</a>
                <a>Contact</a>
            </nav>
        </header>
    );
}

export default Header