import "./Header.css"

function Header() {

    return(
        <header class="top-bar">
            <nav>
                <a href="#">Home</a>
                <a href="#">Experience</a>
                <a href="#contact">Contact</a>
            </nav>
        </header>
    );
}

export default Header